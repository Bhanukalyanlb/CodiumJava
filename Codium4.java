In exception handling else statements blocks will be executed when there is no exception occurs.
Finally statements will be execute statements in all the cases whether it is exception or non-exception.


	else:
		If there is no exception then executes the block
	Finally:
		This will executes all the statements (exception or no exception)
